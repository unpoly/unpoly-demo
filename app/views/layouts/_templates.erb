<%= render 'placeholders/templates' %>

<template id="offline-modal">
  <h3 class="mb-4">You are offline</h3>
  <p>Your changes might not be saved.</p>
  <div class="mt-4">
    <button type="button" class="btn btn-danger" id="retry-btn">Retry</button>
  </div>
</template>
