<div class="row mb-4">
  <div class="col">
    <h2>Budget #<%= @budget.id %></h2>
  </div>

  <div class="col-sm-auto">
    <%= link_to 'Edit', [:edit, @budget], class: 'btn btn-primary', 'up-follow': '' %>
    <%= link_to 'Delete', @budget, method: :delete, 'up-follow': '', 'up-confirm' => 'Really delete?', class: 'btn btn-danger mr-3'  %>
  </div>
</div>

<dl>
  <dt>Name</dt>
  <dd><%= @budget.name %></dd>
  <dt>Amount</dt>
  <dd><%= @budget.amount %> €</dd>

  <dt>Project</dt>
  <dd>
    <% if @budget.project %>
      <%= link_to @budget.project.name, @budget.project, 'up-layer': 'new', 'up-dismiss-location': projects_path %>
    <% else %>
      &mdash;
    <% end %>
  </dd>
</dl>
