<%= render 'tasks/head', title: 'Tasks' %>

<div id="tasks" class="mt-5">
  <%= render 'tasks/form', task: Task.new, autofocus: true %>

  <% @tasks.each do |task| %>
    <%= render 'tasks/task', task: task %>
  <% end %>
</div>
